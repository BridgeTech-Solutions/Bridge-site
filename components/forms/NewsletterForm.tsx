/**
 * FORMULAIRE NEWSLETTER
 *
 * Utilise dans le Footer pour l'inscription a la newsletter.
 *
 * TODO (Stagiaire 3) :
 * - Champ email simple + bouton "S'abonner"
 * - Validation : email obligatoire et format valide
 * - Envoi vers /api/newsletter (POST) avec { email }
 * - Afficher un message de succes : "Merci pour votre inscription !"
 * - Afficher une erreur si deja inscrit ou email invalide
 * - Reinitialiser le champ apres succes
 *
 * Utiliser : Input, Button de components/ui/
 */

"use client";

export function NewsletterForm() {
  return (
    <form>
      {/* TODO : implementer le formulaire newsletter */}
    </form>
  );
}
