/**
 * FORMULAIRE DE DEMANDE DE DEVIS
 *
 * TODO :
 * - Champs :
 *   - Nom de l'entreprise (obligatoire)
 *   - Nom du contact (obligatoire)
 *   - Email (obligatoire)
 *   - Telephone
 *   - Type de service (select : liste des 6 services BTS)
 *   - Budget approximatif (select : tranches)
 *   - Description du besoin (textarea, obligatoire)
 *   - Delai souhaite (date ou select)
 * - Envoi vers /api/quote (POST)
 * - Message de succes apres envoi
 *
 * Utiliser : Input, Textarea, Button, Label, Select de components/ui/
 * Donnees services : SERVICES depuis lib/constants.ts
 */

"use client";

export function QuoteForm() {
  return (
    <form>
      {/* TODO : implementer le formulaire de devis */}
    </form>
  );
}
