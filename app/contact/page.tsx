/**
 * PAGE CONTACT - Bridge Technologies Solutions
 *
 * TODO Stagiaire 1 :
 * - Formulaire de contact (nom, email, téléphone, sujet, message)
 * - Validation des champs côté client
 * - Envoi vers l'API /api/contact (POST)
 * - Message de succès/erreur après soumission
 * - Informations de contact à droite (email, téléphone, adresse)
 * - Carte Google Maps (optionnel)
 *
 * Composants à utiliser : Input, Button, Textarea de components/ui/
 * API : POST /api/contact avec { name, email, phone, subject, message }
 */

export default function ContactPage() {
  return (
    <main className="pt-20">
      <h1>Page Contact</h1>
      <p>À implémenter - Voir les TODO ci-dessus</p>
    </main>
  );
}
